# Cursor collector

Inventories Cursor permission posture and local project/transcript signals from Cursor state and projects dirs.

| | |
|---|---|
| Evidence | `evidence/cursor.json` |
| In `aiscan all` | yes |
| Deps | stdlib only |

## Test

```powershell
.\scripts\test-component.ps1 -Name cursor
.\aiscan.ps1 cursor
```