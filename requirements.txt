# These deps are for pii-scan ONLY. The other 8 collectors are pure stdlib
# (secrets-scan shells out to the gitleaks binary, not a pip dep). You do not need
# a venv or pip install to run claude/cursor/codex/copilot/grok/chat-history/
# git-posture/secrets-scan.
# Only set up a venv here if you will run the PII scan:
#   python -m venv .venv; .\.venv\Scripts\Activate.ps1
#   pip install -r requirements.txt
#   python -m spacy download en_core_web_lg
presidio-analyzer>=2.2.355
presidio-anonymizer>=2.2.355
spacy>=3.7.0
